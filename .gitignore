# Local configuration (credentials) — generated by the installer
/config/app.php

# User uploads (product images) — back these up separately
/public/assets/img/uploads/*
!/public/assets/img/uploads/.gitkeep
!/public/assets/img/uploads/.htaccess

# OS / editor noise
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp

# Logs, dumps, backups
*.log
/backups/
*.sql.gz
dump*.sql

# Stray empty repo created by accident
/Untitled/
